<div class="clear"></div>


<p id="notice"><%= notice %></p>

<h1>Spotyfi</h1>
<section id="sidebar">

  <h2>Listen</h2>
  <section id="audio">
    No song is currently playing


  </section>
</section>
<section id="main">

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.key %></td>
        <td><%= link_to "Play", song.public_url(), :class => "play_song" %></td>

      </tr>
    <% end %>
  </tbody>
</table>
</section>
<br>

<%= link_to 'New Song', new_song_path %>
